import React from "react";
import styled from "styled-components";

const Filter = () => {
  return <StyledFilterDiv>필터</StyledFilterDiv>;
};

const StyledFilterDiv = styled.div`
  padding: 10px 7px;
`;

export default Filter;
